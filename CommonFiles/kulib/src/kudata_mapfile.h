

#ifndef __KUDATA_MAPFILE
#define __KUDATA_MAPFILE

#include "kudata.h"

KU_CDECL_BEGIN

int kudata_mapfile( kudata_t* data, const char* filename );
void kudata_unmapfile( kudata_t* data );

KU_CDECL_END

#endif // __KUDATA_MAPFILE

